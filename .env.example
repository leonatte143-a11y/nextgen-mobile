# Copy to ".env" in this folder (NEXGEN/mobile-app root), then restart Metro: npx expo start --clear
#
EXPO_PUBLIC_API_URL=http://192.168.0.142:4000

# 1 = call real backend (default)
EXPO_PUBLIC_USE_API=1
